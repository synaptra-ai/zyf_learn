# 生产环境变量 — 部署到 ECS 时填写
# 复制此文件为 .env 并填入实际值

DB_USER=booknest
DB_PASSWORD=          # 用 openssl rand -base64 32 生成
DB_NAME=booknest
JWT_SECRET=           # 用 openssl rand -base64 32 生成
DOMAIN=zyfcloud.cn
CORS_ORIGINS=https://zyfcloud.cn,https://www.zyfcloud.cn
