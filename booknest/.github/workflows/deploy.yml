# 此文件位于 booknest/.github/workflows/deploy.yml
# GitHub Actions 只识别仓库根目录 .github/workflows/ 下的 workflow 文件。
# 由于本文件在 booknest/ 子目录中，GitHub Actions 不会执行它。
#
# 实际使用的 workflow 在仓库根目录：
#   .github/workflows/deploy.yml  — GitHub-hosted runner + SSH 部署
#   .github/workflows/e2e.yml     — GitHub-hosted runner + Playwright E2E
#
# 参见 tasks/day4-deploy.md 了解部署方案详情。

name: Deploy (not executed — see root .github/workflows/)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs: false
    steps:
      - run: echo "This file is not executed by GitHub Actions. See .github/workflows/ at repo root."
