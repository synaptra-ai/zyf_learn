# 已弃用
# 本文件是旧版 GitHub-hosted runner + SSH 部署方案的工作流。
# 实际使用的 workflow 已迁移到仓库根目录 .github/workflows/deploy.yml
# 使用 ECS Self-hosted Runner 方案。参见 tasks/day4-deploy-v2-self-hosted-runner.md。
#
# 此文件保留仅供参考，GitHub Actions 不会执行此文件
# （因为 workflows 需要在仓库根目录 .github/workflows/ 下才会被识别）。

name: Deploy to Production (DEPRECATED)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs: false
    steps:
      - run: echo "This workflow is deprecated. See .github/workflows/deploy.yml at repo root."
